spring:
  main:
    web-application-type: none   # a console app, not a server
  ai:
    mcp:
      client:
        streamable-http:
          connections:
            openfilz:
              # Your OpenFilz MCP endpoint (your deployment's API hostname).
              # Override with OPENFILZ_MCP_URL.
              url: ${OPENFILZ_MCP_URL:https://openfilz-api.yourdomain.com/mcp}

# The bearer token is added by OpenFilzAuthCustomizer (see the Java source).
openfilz:
  mcp:
    token: ${OPENFILZ_TOKEN:}
