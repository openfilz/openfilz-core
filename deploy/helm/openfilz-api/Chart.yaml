apiVersion: v2
name: openfilz-api
description: >-
  OpenFilz Community Edition API (REST + GraphQL). Deployable standalone
  (bring your own PostgreSQL / Keycloak) or as a subchart of the openfilz-ce
  umbrella chart, which wires the database/host/auth conventions through the
  `global:` values block.
type: application
version: 1.1.0
appVersion: "latest"
keywords:
  - openfilz
  - dms
home: https://openfilz.com
sources:
  - https://github.com/openfilz/openfilz-core
