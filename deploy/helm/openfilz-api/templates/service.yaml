apiVersion: v1
kind: Service
metadata:
  name: {{ include "openfilz-api.fullname" . }}
  labels: {{- include "openfilz-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector: {{- include "openfilz-api.selectorLabels" . | nindent 4 }}
